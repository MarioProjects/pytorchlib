 - RECORDATORIO: Añadir atributo 'net_type' a todas las redes ("fully-connected", "convolutional")
 - Hacer posible que pasemos una lista de configuracion de la red para inicializarla (VGG, ResNet, DenseNet, BasicNets)
 - Comentar todo estilo PEP8 (mirar proyecto Felix?)
 - Comprobar cuda available para usar cuda y torch.cuda.floatensor y demas -> 
            device = torch.device("cuda:0" if torch.cuda.is_available() else "cpu") DEPUES model.to(device)
 - Añadir ruido a VGG y demás
 - Añadir dropout a mobilenetv2 y basicnets